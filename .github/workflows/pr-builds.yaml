name: Rust

permissions:
  contents: write

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_INCREMENTAL: 0
  CARGO_NET_GIT_FETCH_WITH_CLI: true
  CARGO_NET_RETRY: 10
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTFLAGS: -D warnings
  RUSTUP_MAX_RETRIES: 10

jobs:
  Build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Install rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install rust target
        run: |
          cargo install cargo-generate-rpm just
          rustup target add ${{ matrix.target }}

      - uses: taiki-e/setup-cross-toolchain-action@v1
        with:
          target: ${{ matrix.target }}
        if: startsWith(matrix.os, 'ubuntu') && !contains(matrix.target, '-musl')

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y --no-install-recommends libc6-dev libfindbin-libs-perl make gcc-aarch64-linux-gnu libc6-dev-arm64-cross luajit libluajit-5.1-dev
        if: startsWith(matrix.os, 'ubuntu')

      - name: Build Niji ${{ matrix.target }}
        run: just build

      - name: Upload application binary
        uses: actions/upload-artifact@v7
        with:
          name: niji-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/niji
          if-no-files-found: error
          retention-days: 7
          compression-level: 0
          overwrite: false
          include-hidden-files: false
        if:
          ${{ matrix.target == 'aarch64-unknown-linux-gnu' || matrix.target ==
          'x86_64-unknown-linux-gnu' }}

      - name: Build RPM package
        run: just build-rpm-target ${{ matrix.target }}
        if:
          ${{ matrix.target == 'aarch64-unknown-linux-gnu' || matrix.target ==
          'x86_64-unknown-linux-gnu' }}

      - name: Upload RPM package
        uses: actions/upload-artifact@v7
        with:
          name: niji-${{ matrix.target }}.rpm
          path: target/${{ matrix.target }}/generate-rpm/*.rpm
          if-no-files-found: error
          retention-days: 7
          compression-level: 0
          overwrite: false
          include-hidden-files: false
        if:
          ${{ matrix.target == 'aarch64-unknown-linux-gnu' || matrix.target ==
          'x86_64-unknown-linux-gnu' }}
